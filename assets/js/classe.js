btnDelClasses = document.querySelectorAll(".del-classe");
console.log(btnDelClasses);

//===============

btnDelClasses.forEach(btnDelClasse => {
    btnDelClasse.addEventListener("click", (e) =>{

    })
});