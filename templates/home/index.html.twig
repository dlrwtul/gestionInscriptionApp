{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<div class="w-full p-8 flex flex-row justify-around">
    <div class="stack w-1/3">
    <div class="stat grid rounded w-11/12 h-52 mx-4 rounde  bg-gray-900 text-primary-content place-content-center">
        <div class="stat-title text-2xl">Nombre total d'etudiants</div>
        <div class="stat-value my-2 ml-3">{{ total }}</div>
        <div class="stat-desc ">Annee scolaire {{ app.session.get("anneeEnCours") }}</div>
    </div> 
    <div class="grid w-11/12 h-52 mx-4 rounded bg-gray-600 text-accent-content place-content-center relative top-2">2</div> 
    <div class="grid w-11/12 h-52 mx-4 rounded bg-gray-500 text-secondary-content place-content-center relative top-4">3</div>
    </div>
    <div class="stack w-1/3">
    <div class="stat grid rounded w-11/12 h-52 mx-4 rounde  bg-gray-800 text-primary-content place-content-center">
        <div class="stat-title text-2xl">Total etudiants masculin</div>
        <div class="stat-value my-2 ml-3">{{ totalM }}</div>
        <div class="stat-desc ">pourcentage {{mpourc}}</div>
        <div class="stat-desc py-3"><progress class="progress progress-info w-56" value="{{ totalM }}" max="{{ total }}"></progress></div>
    </div>
    <div class="grid w-11/12 h-52 mx-4 rounded bg-gray-500 text-accent-content place-content-center relative top-2">2</div> 
    <div class="grid w-11/12 h-52 mx-4 rounded bg-gray-400 text-secondary-content place-content-center relative top-4">3</div>
    </div>
    <div class="stack w-1/3">
    <div class="stat grid rounded w-11/12 h-52 mx-4 rounde  bg-gray-800 text-primary-content place-content-center">
        <div class="stat-title text-2xl">Total etudiants masculin</div>
        <div class="stat-value my-2 ml-3">{{ totalF }}</div>
        <div class="stat-desc ">pourcentage {{fpourc}}</div>
        <div class="stat-desc py-3"><progress class="progress progress-info w-56" value="{{ totalF }}" max="{{ total }}"></progress></div>
    </div>
    <div class="grid w-11/12 h-52 mx-4 rounded bg-gray-500 text-accent-content place-content-center relative top-2">2</div> 
    <div class="grid w-11/12 h-52 mx-4 rounded bg-gray-400 text-secondary-content place-content-center relative top-4">3</div>
    </div>
</div>
{# <div class="w-full h-40">
    {{ render_chart(chart, {'class': 'my-chart'}) }}
</div> #}

{% endblock %}
